<% flash.each do |message_type, message| %>
  <div class="alert alert-<%= message_type %>"><%= message %></div>
<% end %>

<div class="container">
    <div class="row mt-5 justify-content-center">
      <div class="font"><h4>ジャンル一覧&追加</h4></div>
    </div>
</div>

  <%= form_with model: @genre,url: admin_genres_path do |f| %>
    <div class="container">
      <div class="row mt-5 justify-content-center">
        <table>
          <tr><th>ジャンル名</th></td>
          <td><%= f.text_field :name, placeholder: "ジャンル名" %></td>
          <td><%=f.submit '新規投稿', class: "btn btn-success" %></td></tr>
          </table>
        </div>
      </div>
  <% end %>


    <div class="container text-center">
      <div class="row">
        <div class=col-md-4>
          <table class="table">
          <thead>
            <tr>
            <th>ジャンル</th>
            <th></th>
            <th colspan="2"></th>
            </tr>
          </thead>
          <tbody>
            <% @genres.each do |genre| %>
              <tr>
                <td><%= genre.name %></td>
                <td><%= link_to "編集する", edit_admin_genre_path(genre.id), class: "btn btn-sm btn-success" %></td>
              </tr>
            <% end %>
          </tbody>
          </table>
        </div>
      </div>
    </div>

<div class="row mt-2 mb-5 justify-content-center">
  <%= link_to "Homeへ戻る", admin_top_path %>
</div>
</div>