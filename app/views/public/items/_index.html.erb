<div class="container text-center">
    <div class="row mt-5 justify-content-center">
      <h4>商品一覧</h4>
    </div>

<div class="row mt-3 mb-3 justify-content-center">
<div class="input-group col-4">
  <input type="text" class="form-control" placeholder="キーワードを入力">
  <button class="btn btn-primary" type="button" id="button-addon2"><i class="fas fa-search"></i> 検索</button>
</div>
</div>

    <table class="table table-hover table-bordered table-sm">
    <thead>
      <th>お気に入り</th>
      <th>商品画像</th>
      <th>ジャンル</th>
      <th>商品名/型/サイズ</th>
      <th>数量</th>
      <th>定価</th>
    </thread>

    <% if customer_signed_in? %>

  <tbody>
      <% @items.each do |item| %>

    <tr>
      <td><i class="fa-regular fa-star fa-2x mt-3 "></i></td>
      <td>
      <%= link_to public_item_path(item.id) do %>
      <%= image_tag item.image, size: '50x50'%>
      <% end %>
      </td>
      <td><%= item.genre.name %></td>
      <td><%= link_to public_item_path(item.id) do %>
      <%= item.name %><br />
      <%= item.shape %><br />
      <%= item.size %>
      <% end %>
      </td>
      <td><%= item.amount %></td>
      <td>¥<%= item.last_price %></td>
      <%# if @item.favorited_by?(current_customer) %>
      <td><%#= link_to "ブックマークを外す", item_favorites_path(@item), method: :delete %></td>
    <% #else %>
      <td><%#= link_to "ブックマーク", item_bfavorites_path(@item), method: :post %></td>
    <% #end %>
            </div>
            </div>
            </div>
        <% end %>

  <% end %>






 <% if general_customer_signed_in? %>

  <tbody>
      <% @items.each do |item| %>

    <tr>
      <td><i class="fa-regular fa-star fa-2x mt-3 "></i></td>
      <td>
      <%= link_to general_public_item_path(item.id) do %>
      <%= image_tag item.image, size: '50x50'%>
      <% end %>
      </td>
      <td><%= item.genre.name %></td>
      <td><%= link_to general_public_item_path(item.id) do %>
      <%= item.name %><br />
      <%= item.shape %><br />
      <%= item.size %>
      <% end %>
      </td>
      <td><%= item.amount %></td>
      <td>¥<%= item.last_price %></td>
      <%# if @item.favorited_by?(current_general_customer) %>
      <td><%#= link_to "ブックマークを外す", item_favorites_path(@item), method: :delete %></td>
    <% #else %>
      <td><%#= link_to "ブックマーク", item_bfavorites_path(@item), method: :post %></td>
    <% #end %>
            </div>
            </div>
            </div>
        <% end %>

  <% end %>





    </div>



