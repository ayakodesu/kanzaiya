
<%= render 'index', orders: @orders %>

<div class="row mt-3 mb-3 justify-content-center">
  <div class="col-lg-4 col-md-5 mb-3 text-center">
  <%= form_tag(public_orders_path, method: :get) do %>
    <%= text_field_tag :search, '', placeholder: "現場名検索" %>
    <%= submit_tag '現場名検索', spot: nil %>
  <% end %>
  </div>

  <div class="col-lg-4 col-md-5 mb-3 text-center">
  <%= form_tag(public_orders_path, method: :get) do %>
    <%= text_field_tag :search, '', placeholder: "フルネーム検索" %>
    <%= submit_tag '配送先名検索', name: nil%>
    <%= hidden_field_tag 'search_method', "shipping_address" %>
  <% end %>
  </div>

  <div class="col-lg-4 col-md-5 mb-3 text-center">
  <%= form_tag(public_orders_path, method: :get) do %>
    <%= text_field_tag :search, '', placeholder: "住所検索" %>
    <%= submit_tag '住所検索', address: nil %>
  <% end %>
  </div>
</div>

<div class="row mb-3 justify-content-center">
  <%#= form_tag(public_orders_path, method: :get) do %>
    <%#= text_field_tag :search, '', placeholder: "キーワード検索", :size=>"50" %>
    <%#= submit_tag 'キーワード検索', name: nil, spot: nil, address: nil %>
    <%#= hidden_field_tag 'search_method', "spot" %>
    <%#= hidden_field_tag 'search_method', "name" %>
    <%#= hidden_field_tag 'search_method', "shipping_address" %>
  <%# end %>
</div>

    <body>
      <% @orders.each do |order| %>
        <tr bgcolor="#f5f5f5">
          <td><%= link_to order.created_at.strftime('%Y年%m月%d日 %H:%M:%S'), public_order_path(order.id) %></td>
          <td><%= order.spot %></td>
          <td><%= order.name %></td>
          <td>〒<%= order.postal_code %><br/>
          <%= order.address %><br/>
          TEL:<%= order.telephone_number %></td>
        </tr>
      <% end %>
    </body>
  </table>

<div class="row mt-2 justify-content-center">
  <%= link_to "Myページへ戻る", public_customers_path %>
</div>

</header>